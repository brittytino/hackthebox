# Database
DATABASE_URL="postgresql://postgres:postgres@db:5432/hackthebox?schema=public"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="24h"

# SMTP Configuration (for OTP emails)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-specific-password"
SMTP_FROM="Operation Cipher Strike <noreply@hackthebox.com>"

# Frontend URL
FRONTEND_URL="http://localhost:3000"

# Event Configuration
EVENT_DATE="February 1, 2026"
EVENT_NAME="Operation Cipher Strike"

# Redis (for rate limiting and real-time features)
REDIS_HOST="redis"
REDIS_PORT=6379

# Server
PORT=4000
NODE_ENV="development"
